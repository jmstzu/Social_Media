import { createTheme } from '@mui/material/styles';

export default createTheme({
  palette: {
    primary: {
      main: '#000000', // Instagram black
    },
    secondary: {
      main: '#8e8e8e', // Instagram gray
    },
  },
});